generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Meetup {
  id          Int     @id @default(autoincrement())
  title       String?
  address     String?
  description String?
  image       String?
}
